{
  "name": "react-film-server",
  "version": "1.0.0",
  "description": "The `react-qa-server` is the server-side app companion of HeapOverrun (i.e., `react-qa`). It presents some APIs to perform some CRUD operations on questions and their answers.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dayjs": "^1.11.7",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "sqlite3": "^5.1.6"
  }
}
